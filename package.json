{
  "name": "slambda",
  "version": "0.1.1",
  "description": "TypeScript helpers for implementing AWS Lambda handlers",
  "main": "slambda",
  "typings": "slambda",
  "scripts": {
    "clean": "npx shx rm -rf dist",
    "build": "npx shx mkdir -p dist && npx shx cp -f package.json dist && tsc -p .",
    "watch": "tsc -p . -w"
  },
  "author": "Jared Krinke",
  "license": "MIT",
  "dependencies": {
    "@types/aws-lambda": "^8.10.39"
  },
  "devDependencies": {
    "@types/node": "^13.1.2",
    "shx": "^0.3.2"
  },
  "files": [
    "slambda.ts",
    "slambda.d.ts"
  ]
}
